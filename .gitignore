# Compiled binary (only the root binary, not the cmd/keymaprd/ source directory)
/keymaprd

# Go build cache
*.test
*.out

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Go vendor directory (if used)
vendor/
